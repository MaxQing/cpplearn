#include <iostream>

int main() {
    std::cout << "4.4 result is : " << 12 / 3 * 4 + 5 * 15 + 24 % 4 / 2 << std::endl;
    std::cout << "4.5.1 result is : " << (-30) * 3 + 21 / 5 << std::endl;
    std::cout << "4.5.2 result is : " << (-30) + 3 * 21 / 5 << std::endl;
    std::cout << "4.5.3 result is : " << 30 / 3 * 21 % 5 << std::endl;
    std::cout << "4.5.4 result is : " << (-30) / 3 * 21 % 4 << std::endl;
    return 0;
}